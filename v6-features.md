# v6 Feature Improvements — Benjamin Seda / Atlas Site

Status key: ✅ Done | 🔄 In Progress | ⬜ Pending

---

## F1 — Nav logo + font ✅
- Brand-mark diamond: 22px → 28px
- ATLAS font: 14px → 20px
- Advisory sub: 8px → 10px
- Nav height: 58px → 68px, padding: 48px → 56px
- Enrollment bar + progress bar offsets updated

## F2 — Desktop containers wider + VSL bigger ✅
- `.inner` max-width: 900px → ~1040px
- `.inner.wide` max-width: 1140px → ~1300px
- VSL max-width: 720px → ~860px (slightly wider than hero text)
- Hero should feel desktop-first, not mobile-optimized

## F3 — Gap section: James case study card ✅
- Replace current text block with a Hinge-style dating profile card
- Hyper-realistic AI-generated photo of a wealthy man who doesn't take good dating photos
- Narrative: "This is James. Hyper-successful, good-looking, but has trouble with women."
- Card styled like a Hinge user profile (not exact Hinge clone but clearly inspired)

## F4 — Problem section: James narrative story flow ⬜
- Reframe the 3 broken channels as James's story
- "Here's what was broken for James" → channel 1, channel 2, channel 3
- Flows naturally into "here's how we built his pipeline"
- Story arc: problem → Atlas builds the solution → next section

## F5 — TikTok scroll-snap before/after section ⬜
- Dedicated section with scroll-snap (auto-advances to next card on scroll)
- Section name label visible at top during scroll
- Atlas-style map/progress indicator (like the network SVG) as a progress bar
- Before → After framing per card

## F6 — Results cards wider ⬜
- Tinder card width: 230px → wider (e.g. 280–300px)
- Container max-width wider on desktop
- Cards should breathe more on a large screen

## F7 — About section: Benjamin photo ⬜
- Add a photo placeholder/slot in "Why I walked away from 200 million views" section
- Positioned naturally in the layout (split or above text)

## F8 — Section header + subheader font sizes bigger ⬜
- Eyebrow labels (e.g. "Water", "Why Benjamin Seda") → slightly larger
- Section subheaders (e.g. "Week 1–2" timeline labels) → bigger
- h-sm class and section labels generally bumped up

## F9 — Feedback layer headers bigger ⬜
- Phase header ("The Feedback Layer") font size up
- Subheader inside that section up
- The inline feature card headers (Direct Line, Live Sessions, Atlas Network) bigger
